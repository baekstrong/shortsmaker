# 중앙 고정·직접 위치 조정 검증

2026-09-10 사용자 요청 반영.

- 기본150%·중앙 고정, 과거 framing/end_center에 좌우 이동 데이터가 있어도 적용하지 않음. 직접 조정값만 쇼츠 전체에 고정 적용.
- 브라우저에서 왼쪽 정렬→center0/left0px, 오른쪽 정렬→center1/left-116px 확인. 가운데 정렬과150%초기화 확인.
- 실제 마우스30px오른쪽/20px아래 드래그→center0.413793, vertical0.592365 저장. 새로고침 후 동일값/동일CSS좌표 유지.
- 동일값으로 원본30~33초를 실제 인코딩:1080×1920, 확대된 원본1620×912, 크롭x130/배치y596. 미리보기와 출력 프레임 시각 대조 일치. 테스트 결과물은 data/verification/fixed-position/에 보관(Git 제외).
- pytest22개 통과. 레거시 AI 이동 무시, 정보 제안이 수동 위치와 렌더 키를 바꾸지 않음, 세로 위치 검증, 실제FFmpeg 출력 픽셀의 세로 경계 및 오디오 싱크 검사 포함. JS문법 검사 통과, 브라우저 JS오류 없음.
- 실제 Astra medium으로 수업4번 쇼츠(394.10~527.66초)13개 프레임 분석.483.14초에서 ATP-PC/해당과정 판서 왼쪽 잘림을 제안. 얼굴 위치가 아닌 판서 내용 기준.
- 제안 클릭→영상483.14초로 이동, manual_frame=null/150%·중앙 고정 유지. 권장100%는 화면에 안내만 하고 자동 적용하지 않음.
- 검증 후 샘플16개 모두 manual_frame=null로 확인. 테스트로 바꾼 값은 중앙 기본값 복원. 기존 MP4와 사용자 문구 승인 상태는 보존, 외부 예약/업로드/발행 수행 안 함.

AI는 대표 프레임을 분석하므로 모든 순간의 정보 잘림을 보장하지 않는다. 제안 확인 후 사용자가 직접 조정한다. 이번 검증은 수업 구간 하나의 실제AI호출이며 전체16개 재분석/재인코딩은 수행하지 않았다.
