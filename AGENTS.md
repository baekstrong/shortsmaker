# shortsmaker 작업 지침

## 프로젝트 목표
- 롱폼 영상을 쇼츠로 자동 편집·생성하는 앱을 만든다.
- 초기 실사용 베타를 구현 중이다. 확정 범위는 docs/product-spec.md를 따른다. 실제 발행 테스트는 하지 않고 예약 생성·조회 후 즉시 삭제한다.

## 매 작업 자동 커밋·푸시
- 사용자가 별도로 중단을 지시하지 않는 한, 파일 변경이 있는 각 작업을 마칠 때 관련 검증 → diff 확인 → commit → push를 완료한다. 별도 승인을 반복해서 묻지 않는다.
- 작업 시작 시 브랜치·원격·작업 트리 상태를 확인한다. 기존 변경을 덮어쓰지 않는다.
- 이번 작업에서 직접 변경한 파일만 명시적으로 stage한다. 다른 작업의 변경을 임의로 커밋하지 않는다.
- 커밋 메시지는 변경 목적이 드러나는 한글로 작성한다.
- 현재 작업 브랜치로 push하고, upstream이 없으면 origin의 같은 이름 브랜치에 설정한다.
- 비밀번호·API 키·토큰·환경변수 비밀값·원본 영상·대용량 생성물은 커밋하지 않는다.
- force push, 이력 삭제, 충돌 덮어쓰기를 하지 않는다. push 또는 검증 실패 시 원인을 알리고 미완료 상태를 명시한다.
- 파일 변경이 없는 회의·질의응답에는 빈 커밋을 만들지 않는다. 확정된 결정은 문서에 반영한 뒤 커밋·푸시한다.
- 최종 응답에 작업 결과와 커밋·푸시 상태를 간단히 보고한다.

## 공유 위키
- 정본은 `~/.claude/wiki/`이며 `~/.codex/wiki`는 같은 폴더를 가리킨다.
- 시작 시 `git -C ~/.claude/wiki status -sb`를 확인하고, 깨끗하며 병합·리베이스 중이 아닐 때만 `pull --ff-only`한다.
- `WIKI.md`와 `index.md`를 먼저 읽고 관련 페이지만 필요할 때 읽는다.
- 의미 있는 진행과 최종 응답 전에 결정·이유·진행 상황·다음 행동·산출물 위치를 날짜와 출처와 함께 기록한다. 비밀값은 기록하지 않는다.
- 기존 페이지를 우선 갱신하고 index.md와 log.md를 유지한다. diff와 충돌을 확인하고 직접 수정한 파일만 한글 메시지로 commit 후 push한다. 실패는 숨기지 않는다.

## iTerm2 pane 타이틀
- 주 에이전트는 새 요청 첫 도구 호출에 `sh ~/.codex/scripts/panetitle.sh set '<15자 내외 작업 요약>' '<프로젝트 절대경로>'`를 실행한다.
- 프로젝트 루트의 절대경로를 사용한다. 단계가 크게 바뀌면 갱신하며 완료 후에도 유지한다.
- 하위 에이전트는 제목을 바꾸지 않는다. 완료 소리는 별도로 호출하지 않는다.
